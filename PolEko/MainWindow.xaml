<Window x:Class="PolEko.MainWindow"
        xml:lang="pl-PL"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PolEko"
        mc:Ignorable="d"
        Title="Poleko">
  <Grid Name="Grid">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1*" />
      <ColumnDefinition Width="5*" />
    </Grid.ColumnDefinitions>
    
    <local:SideMenu Margin="5" Devices="{Binding Devices, RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}"/>
    
    <GridSplitter Grid.RowSpan="3" Width="3"/>
    <TabControl Grid.Row="0" Grid.Column="1" Name="TabControl"/>
  </Grid>
</Window>